import { PagePlaceholder } from "@/components/scaffold/PagePlaceholder";

export default function AuthActionPage() {
  return (
    <PagePlaceholder
      title="Auth action handler"
      phase="Phase 5"
      meta={{ note: "?mode=resetPassword | verifyEmail | recoverEmail" }}
    />
  );
}
